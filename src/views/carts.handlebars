<div>
    <h1>Carts List</h1>
</div>
<div>
    {{#if isAdmin}}

        <ul>
            {{#each carts}}
            <hr>
            <li>{{id}}</li>
            <li>{{products}}</li>
            {{/each}}
        </ul>
    {{else}}
    <div>No hay carritos</div>
    {{/if}}
</div>